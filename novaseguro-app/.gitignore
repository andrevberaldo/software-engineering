# Node
node_modules/
.next/
npm-debug.log*

# Python
.venv/
__pycache__/
*.pyc

# Env files reais (mantenha só os .env.example no repo)
.env
.env.local
backend/.env
bff/.env
frontend/.env.local

# Armazenamento local de PDFs (fora do Docker, sem volume)
backend/data/

# SO / editores
.DS_Store
